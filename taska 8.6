word = 'bucefal'
def isogram(word):
    if word.split() and word.isalpha():
        return 'this is isogram'
    
    elif word.split() and word.isdigit():
        return 'text is not isogram'
    
    else:
        'try again'
p = isogram(word)
print(p)
